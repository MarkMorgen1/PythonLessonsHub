# define function for calculating deflection
import numpy as np

# def deflection_at_points(P, L, a, delta_x):
#     b = L - a
x_vals = np.arange(0, 8, 0.85)
print(x_vals)
print(type(x_vals))
y_vals = 2 * x_vals
print(y_vals)
